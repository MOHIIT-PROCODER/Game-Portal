{
  "name": "game-portal-workspace",
  "version": "1.0.0",
  "description": "CrazyGames-like Game Portal Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run install:all && node -e \"const { spawn } = require('child_process'); spawn('npm', ['run', 'dev:server'], { stdio: 'inherit', shell: true }); spawn('npm', ['run', 'dev:client'], { stdio: 'inherit', shell: true });\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
